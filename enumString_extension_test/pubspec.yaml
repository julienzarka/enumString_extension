name: enumString_extension_test
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  redux: ^4.0.0
  flutter:
    sdk: flutter
  enumString_extension:
    path: ../enumString_extension

dev_dependencies:
  build_runner: ^1.7.2
  flutter_test:
    sdk: flutter
  enumString_extension_gen:
    path: ../enumString_extension_gen
    
dependency_overrides:
  enumString_extension:
    path: ../enumString_extension

flutter:
  uses-material-design: true
